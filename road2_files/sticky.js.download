jQuery(function(t){var o=t("#sidebar"),s=t(window);offset=o.offset(),sidebar_height=o.height()+600,s.scroll(function(){content_height=t(".innerpost-left-sec").height(),content_offset=t(".innerpost-left-sec").offset(),topPadding=80,t("#sidebar_out").css("height",content_height+"px"),s.scrollTop()>offset.top&&content_height+content_offset.top>s.scrollTop()+sidebar_height?(o.stop().css("position","fixed"),o.stop().css("top","60px"),o.stop().css("bottom","")):s.scrollTop()<offset.top?(o.stop().css("position","absolute"),o.stop().css("bottom",""),o.stop().css("top","0px")):(o.stop().css("position","absolute"),o.stop().css("bottom","0px"),o.stop().css("top",""))})});
// ################# LAZY LOAD CALL FOR IMAGES ##################################
document.onscroll=function(){
   var imgDefer = document.getElementsByTagName('img');
   for (var i=0; i<imgDefer.length; i++) {
           if(imgDefer[i].getAttribute('data-lazy-src')) {
               imgDefer[i].setAttribute('src',imgDefer[i].getAttribute('data-lazy-src'));
           }
   }
};
//function init() {
   var imgDefer = document.getElementsByTagName('img');
   for (var i=0; i<3; i++) {
       if(imgDefer[i].getAttribute('data-lazy-src')) {
           imgDefer[i].setAttribute('src',imgDefer[i].getAttribute('data-lazy-src'));
       }
   }
jQuery('.innerpost-left-sec a img').each(function(n){
   var c = jQuery(this).parent();
   if(c.is("a")) {
        jQuery(this).parent().addClass('parent_atag');
   }
 });
//}
//window.onload = init;

var lastScrollTop=0;
jQuery(window).scroll(function(event){
	var currentScroll=jQuery(this).scrollTop();
	if (jQuery(window).width() > 800){
		if (currentScroll < lastScrollTop && currentScroll > 150 )
	  	{
	    	jQuery('.stick-section').stop(true,true).slideDown('slow');
	    	lastScrollTop=currentScroll;
	  	}
	  	if (currentScroll > lastScrollTop )
	  	{
	  		jQuery('.stick-section').stop(true,true).slideUp('slow');
	    	lastScrollTop=currentScroll;
	  	}
	  	if (currentScroll < 150 )
	  	{
	  		jQuery('.stick-section').stop(true,true).slideUp('slow');
	    	lastScrollTop=currentScroll;
	  	}
	}
});
// ################# LAZY LOAD CALL FOR IMAGES ##################################
